BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Indikator] ALTER COLUMN [is_benefit] TINYINT NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
