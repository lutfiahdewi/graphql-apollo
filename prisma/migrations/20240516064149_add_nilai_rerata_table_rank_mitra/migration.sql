BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[RankMitra] ADD [nilai_rerata] DECIMAL(3,2);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
